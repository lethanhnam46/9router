services:
  9router:
    image: decolua/9router:latest
    container_name: 9router
    ports:
      - '20128:20128'
    volumes:
      - 9router-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  9router-data:
